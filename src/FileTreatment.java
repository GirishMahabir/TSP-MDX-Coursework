public class FileTreatment {
    /**
     * FileTreatment is a class that contains methods to fix known issues with the dataset file.
     * For example:
     * 1. Remove the Empty lines in the dataset file.
     * 2. Check if all the numbers are seperated by a tab.
     * 3. Check if the file contains only numbers.
     * 4. Check if the file contains only 3 columns.
     *
     */
}
